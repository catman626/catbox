#!/bin/bash

remote=$1
source=$2

echo remote is $remote
if [[ "$remote" = "huawei" ]]
then 
    sshpass -p Megacat666 scp -r $HUAWEI_IP:/home/catman/FlexAscend/$source .
else
    echo "remote not supported" $remote
fi
